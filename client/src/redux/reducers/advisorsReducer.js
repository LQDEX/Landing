import members from '../../constants/advisorMembers';

const initialState = {
  members
};

/* eslint-disable */
const advisors = (state = initialState, action) => {
  switch (action.type) {

    default: return state;
  }
};

export default advisors;