import members from '../../constants/investorMembers';

const initialState = {
  members
};

/* eslint-disable */
const team = (state = initialState, action) => {
  switch (action.type) {

    default: return state;
  }
};

export default team;